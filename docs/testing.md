# Testing Guide

- Unit tests: `pytest tests/unit`
- Integration tests: `pytest -m "integration"`
- Performance tests: `pytest -m "performance"`
- Mutation tests (opt-in): `mutmut run`  # requires mutmut to be installed
