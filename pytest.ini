[pytest]
testpaths = tests
addopts = -q

markers =
    slow: marks tests that hit the full data pipeline/backtester (deselect with "-m 'not slow'")
    integration: integration / end-to-end tests
    performance: performance / benchmark tests
    property: property-based tests
